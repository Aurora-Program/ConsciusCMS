# Frontend Environment Variables (EspiralML Public App)
# Copia este archivo a `.env.local` y rellena los valores.
# Precedencia de Vite: .env.local > .env.development > .env

# API base para /pages (sin slash final)
VITE_URL_API_PAGES=https://api.example.com/pages

# Bucket estático donde se sirven assets públicos
VITE_URL_API_BUCKET=https://static.example.com
VITE_CONTENT_BUCKET_NAME=content-bucket-name
VITE_CONTENT_BUCKET_URL=https://content.example.com

# (Opcional) Región / Stage para lógica condicional
VITE_AWS_REGION=eu-west-1
VITE_STAGE=dev

# Debug: si pones "true" forzará un log de variables al arrancar
VITE_ENV_DEBUG=true

# NOTA:
# 1. Usa `.env.local` para secretos o endpoints reales (no commitear).
# 2. Asegúrate de ejecutar `npm run dev` dentro de `/frontend` para que Vite cargue estas vars.
# 3. Si ves en consola API URL: undefined -> falta este archivo o la var.
